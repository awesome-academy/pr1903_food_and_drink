$("#consumable_modal").html("<%= j render 'consumables/edit' %>");
$("#consumable_modal_edit").modal("show");
if (<%=  @consumable.errors.present?  %>) {
  $("#myModalBuyRequest").on("click", function () {
      $(".modal-backdrop").fadeOut("fast");
  });
}
